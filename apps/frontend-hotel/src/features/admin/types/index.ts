//export type Role = 'ADMIN' | 'GUEST' | 'HOTEL';

export type Property = {
  id: number;
  name: string;
  category: string;
  categoryId: number;
  subCategory: string;
  subCategoryId: number;
};

/* export enum Role {
  ADMIN = 'ADMIN',
  USER = 'USER',
}
 */
