{
  "name": "apps/frontend-user",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/frontend-user/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/frontend-user"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "apps/frontend-user:build",
        "proxyConfig": "apps/frontend-user/proxy.conf.json"
      },
      "configurations": {
        "development": {
          "buildTarget": "apps/frontend-user:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "apps/frontend-user:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "apps/frontend-user:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "apps/frontend-user:build:development"
        },
        "production": {
          "buildTarget": "apps/frontend-user:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/apps/frontend-user"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/frontend-user/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
